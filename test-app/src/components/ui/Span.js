import styled from 'styled-components';
import {
  color,
  typography,
  space,
} from 'styled-system';

const Span = styled.span`
  font-family: 'Montserrat', sans-serif;
  ${color}
  ${space}
  ${typography}
`;

export default Span;
